# 项目协作约定

## 更新后自动推送 GitHub

用户已持续授权：之后本项目的更新完成后，自动提交并推送到 GitHub，无须逐次确认。

- 远程仓库：`https://github.com/Amanda-Qu/hip.git`，远程名称为 `origin`。
- 当前发布分支：`codex/hip-visualization`。除非用户另行指定，继续在此分支提交、推送。
- 每次完成用户授权的项目修改后，运行与修改相适应的检查，再提交和推送本次相关文件。仅讨论、查看状态或没有文件变化时，不创建空提交。
- 演示的仓库内源码为 `src/hip-planes.html`。修改演示后运行 `python scripts/build.py`，将源码和生成的 `index.html` 一起提交，确保 GitHub 中的页面与源码一致。
- 如果先在对话中的可视化文件完成修改，应先将修改同步到仓库内源码，再构建、检查和推送。
- 提交前查看工作区差异，仅提交本次工作涉及的内容；遵守 `.gitignore`，保留未完成或无关的本地改动。
- 使用说明确切改动的提交消息，正常推送到 `origin/codex/hip-visualization`。遇到远端更新时先拉取并整合，不强制覆盖远端历史。
- 如因权限、网络或无法解决的冲突导致推送失败，保留本地工作，准确说明失败原因与待处理事项；未验证成功前不得声称已推送。
- 完成后简要告知推送结果和提交编号。

## 代码与文档

- 写代码时注释关键功能；涉及算法时说明算法思路，必要时举例。
- 如果生成文档，公式使用真正的 Office Math 结构，文字字体颜色统一为黑色；兼顾 WPS Office 的兼容性与阅读体验，采用清晰、稳定、易读的排版。
